"""ASCII Visualizer — a pygame-based ASCII screensaver and image-to-ASCII tool.

Two modes (generative dot-field idle + image conversion) and three palettes
(dots, grayscale ramp, braille) that can be switched live at runtime.
"""

__version__ = "0.1.0"
